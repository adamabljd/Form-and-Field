export const dynamic = 'force-dynamic';
import { Suspense } from 'react';
import { Brand } from '@/components/app-shell';
import { AuthForm } from '@/components/auth-form';
import { isSupabaseConfigured } from '@/lib/supabase/config';
import { Volleyball } from 'lucide-react';
export default function LoginPage() { return <main className="grid min-h-screen lg:grid-cols-2"><section className="relative hidden overflow-hidden bg-[#273e34] px-16 py-12 text-white lg:flex lg:flex-col"><div className="text-sm font-semibold tracking-widest">FORM & FIELD</div><div className="pitch-grid absolute inset-0 opacity-40"/><div className="relative my-auto"><Volleyball size={85} strokeWidth={0.8} className="mb-10 text-[#c6d4b9]"/><h2 className="text-5xl font-medium leading-tight tracking-[-2px]">Strong on the bar.<br/><span className="text-[#c6d4b9]">Unstoppable<br/>on the pitch.</span></h2><p className="mt-7 max-w-sm text-sm leading-relaxed text-[#c1ccc3]">A training space for the athlete who doesn’t fit in one box. Calisthenics, football, and everything you’re becoming.</p></div><p className="relative text-xs text-[#c1ccc3]">STRENGTH × SKILL × STAMINA</p></section><section className="flex flex-col px-6 py-8 sm:px-16"><Brand/><div className="flex flex-1 items-center justify-center py-14"><Suspense fallback={<p>Loading…</p>}><AuthForm configured={isSupabaseConfigured()}/></Suspense></div><p className="text-center text-[10px] text-muted">Train with intention. Play with confidence.</p></section></main>; }
